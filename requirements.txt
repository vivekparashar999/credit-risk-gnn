# Everything here is standard and CPU-only -- no GPU, no AWS, no heavy graph
# library is required. Versions are the ones this project was developed and
# verified against; nearby versions will also work.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
networkx>=3.0
torch>=2.1          # CPU build is fine
xgboost>=2.0
matplotlib>=3.7     # only used by the optional plotting in the notebook/README
